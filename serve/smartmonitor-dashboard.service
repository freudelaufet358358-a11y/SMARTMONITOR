[Unit]
Description=SmartMonitor dashboard (static HTTP server on :8080)
After=network.target

[Service]
User=@USER@
WorkingDirectory=@WWW@
ExecStart=/usr/bin/python3 -m http.server 8080 --bind 127.0.0.1
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
